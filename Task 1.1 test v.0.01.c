/*
* САНЯ ПРОЧТИ!!!!!
* Test1.1.c
* Данный тест проверяет работоспособность системы функций, созданной моим коллегой
* В каждом закомиченном блоке, кроме первого содержится какой-то баг, который тебе нужно пофиксить. 
* Прочел? Молодец! А теперь иди вниз скрипта и разкоммить return 0;  в конце, иначе у тебя скрипт не будет работать.
* Good luck!
*/
#include <stdio.h> 
int main(void)                 
{	
	//Простой тест. необходимый для демонстрации работы кода. Здесь всё работает
	/*struct list *root1 = NULL;
	root1 = create_root();
	create_bare_list(root1, 10);
	insert_value(root1, 5);
	print_list(root1);
	delete_unit(root1, 7);
	delete_list(root1);*/
	
	
	/*
	struct list *Dio = NULL;
	Dio = create_root();
	create_bare_list(Dio, 6);
	insert_value(Dio, 5);//вводим значения -1; 20000000000000000000000000000; 0; 10; -10, т.е заведомо большее значение и integer преобразует его в другое число
	delete_unit(Dio, 10);//А здесь вводим 0.1 // если это некорректно, то напиши мне
	delete_unit(Dio, 600);//Здесь вводим значение 7. Ошибка в том, что я могу ввести число юнитов больше, чем длина списка. А затем сослаться на элемент вне списка. (Если длина списка 6, а я сошлюсь на 7 -ой)
	find_unit(Dio, 100);// Здесь тоже вводим значение 7. Тот же самый баг.
	print_list(Dio);
	*/
	/*
	struct list *ban = NULL;
	ban = create_bare_list(ban, 6);
	ban = create_bare_list(ban, 6);// Тут я хз, что не так. Но ошибку выдает
	*/
	/*
	struct list *Glad_Valakas = NULL;
	Glad_Valakas = create_root();
	Glad_Valakas = add_to_end(Glad_Valakas, NULL);
	*/

	/*
	struct list *posvyat_rt = NULL;
	posvyat_rt = create_root();
	create_bare_list(posvyat_rt, NULL);//Введи NULL, если это корректно для юзера

	*/
	/*
	struct list *ok_boomer = NULL;
	ok_boomer = create_root();
	create_bare_list(ok_boomer, 6);
	insert_value(ok_boomer, 7);
	print_list(ok_boomer);
	*/
	/*
	struct list *ok_zoomer = NULL;
	ok_zoomer = create_root();
	create_bare_list(ok_zoomer, 6);
	insert_value(ok_zoomer, NULL);//Здесь нужно снова ввести NULL
	print_list(ok_zoomer);
	*/
/*
	struct list *ok_doomer = NULL;
	ok_doomer = create_root();
	read_list(ok_doomer, NULL);//Cнова ввести NULL, если это корректно для юзера
	read_list(ok_doomer, 5);
	//Вопрос: куда исчез ввод остальных значений?
	print_list(ok_doomer);
	*/


//return 0;
}
